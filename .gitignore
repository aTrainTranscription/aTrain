venv
.venv
__pycache__
dist
build
aTrain.egg-info
installer.iss
test.py
aTrain/required_models/*
.DS_Store
.cache
.pytest_cache

# Local dev container overrides (personal, not committed)
.devcontainer/
docker-compose.override.yml

# Dev-container runtime output: models + transcriptions bind-mounted from
# the container to ./data (root-owned). Never committed.
data/

# Environment files (may contain secrets)
.env
.env.local
.env.*.local
.vscode
.nicegui/

# Local state of AI coding agents (shared instructions live in AGENTS.md)
.claude/settings.local.json
.codex/
CLAUDE.local.md
